name: Build QMK Firmware

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: ghcr.io/qmk/qmk_cli

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup QMK
        run: qmk setup -y

      - name: Copy keyboard files
        run: cp -r keyboards/handwired/joystick_controller ~/qmk_firmware/keyboards/handwired/joystick_controller

      - name: Build firmware
        run: cd ~/qmk_firmware && qmk compile -kb handwired/joystick_controller -km default

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: joystick_controller_firmware
          path: ~/qmk_firmware/*.uf2
